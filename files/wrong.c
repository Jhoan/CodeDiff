#include<stdio.h>
#define getc(p) (--(p)->cnt >= 0 \
       ? (unsigned char) \
	   *(p)->ptr++ : _fillbuf(p))

 #define NORMAL 3
#define COMPUESTO NORMAL+1
int main(void){
	int a;
	unsigned long int **ff,*o,i,l[2],(*c)[2];

	unsigned long int d;
	printf("%d",COMPUESTO);
}
float **foo (int (*another)[333]){
	return NULL;

}
