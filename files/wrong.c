#include<stdio.h>
#include<stdlib>
#define getc(p) (--(p)->cnt >= 0 \
       ? (unsigned char) \
	   *(p)->ptr++ : _fillbuf(p))

 #define NORMAL 3
#define COMPUESTO NORMAL+1
float **foo(int (*)[]);
float **bar(int, long long double);
int main(void){
	int a;
	unsigned long int **ff,*o,i,l[2],(*c)[2];
	signed int var;
	signed int ssi
	const volatile long float **var,varn[2][2]


	printf("%d",COMPUESTO);
	for
}
float **bar(int a, long long double b){
	return NULL
}
float **foo (int (*another)[333]){
	return NULL;

}
